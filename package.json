{
  "name": "innersource-atlas",
  "private": true,
  "version": "0.1.0",
  "description": "InnerSource Atlas – map- and chat-driven discovery for InnerSource systems and repositories",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
